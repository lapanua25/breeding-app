name: Deploy to Portal

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout breeding-app
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install --verbose

      - name: Build app
        run: |
          chmod +x node_modules/.bin/vite
          npm run build

      - name: Clone terra-nova-botanica with verbose output
        run: |
          git clone -v https://x-access-token:${{ secrets.GH_PAT }}@github.com/lapanua25/terra-nova-botanica.git terra-nova-botanica
        env:
          GIT_TRACE: 1

      - name: Copy dist to breeding-app folder
        run: |
          mkdir -p terra-nova-botanica/breeding-app
          rm -rf terra-nova-botanica/breeding-app/*
          cp -r dist/* terra-nova-botanica/breeding-app/
          ls -la terra-nova-botanica/breeding-app/

      - name: Commit and push to terra-nova-botanica
        run: |
          cd terra-nova-botanica
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add breeding-app/
          git status
          git diff --cached --name-only
          git commit -m "Update Botanical Breed app - auto deploy from breeding-app" || echo "No changes to commit"
          git push -v https://x-access-token:${{ secrets.GH_PAT }}@github.com/lapanua25/terra-nova-botanica.git main
        env:
          GIT_TRACE: 1
